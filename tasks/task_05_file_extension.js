'use strict';

/**
 * The function returns the extension of the file
 * (the last part of the string, after the last ".")
 * if there is a dot in the fileName, otherwise
 * it returns false.
 *
 * @param {string} fileName The name/path of the file.
 * @returns {string|boolean} The extension of the file
 * without the dot, or false.
 */
function fileExtension(fileName) {
    console.log('alma');
};
module.exports = fileExtension;