'use strict';

/**
 * The function returns the factorial of it's argument.
 *
 * @param {number} n (1 <= n <= 15)
 * @returns {number} Factorial value or 0 if any arguments are not proper
 */
function factorial(n) {
    let factorialOfN;
    /*
     * Your task is to calculate the factorial of the number
     * can be found in the variable n.
     * https://en.wikipedia.org/wiki/Factorial
     * Store the factorial in the factorialOfN variable.
     * Also take into consideration the documentation of the function!
     */
    // PLACE YOUR CODE BETWEEN THIS...
	

    // ...AND THIS COMMENT LINE!
    return factorialOfN;
}
module.exports = factorial;