/**
 * It recieves an array of strings and returns
 * the longest string from it.
 *
 * @param {Array.<string>} strings
 * @returns {string} longest string
 */
function longestString(strings) {

}